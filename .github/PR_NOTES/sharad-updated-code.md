[github-copilot] sharad updated code...

This PR contains a short note and references the changes already applied to workflows:
- Added explicit image pull steps
- Added input validation and container security hardening
- Added Trivy scan steps

If you want these changes reviewed in a single PR that applies the workflow edits, consider reverting the direct commit on `main` and merging via this PR instead.

